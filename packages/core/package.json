{
  "name": "@clmm-autopilot/core",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"(core) no tests yet\"",
    "lint": "echo \"(core) lint handled at repo root\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
