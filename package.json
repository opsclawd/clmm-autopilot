{
  "name": "clmm-autopilot",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.29.3",
  "engines": {
    "node": "22.22.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -C apps/web dev",
    "build": "pnpm -C apps/web build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "e2e:devnet": "pnpm -C packages/solana e2e:devnet",
    "e2e:certify:devnet": "pnpm -C packages/solana e2e:certify:devnet",
    "receipt:build": "node ./scripts/build-receipt.mjs",
    "receipt:check:devnet": "node ./scripts/check-devnet-receipt-consistency.mjs",
    "receipt:check:mainnet": "node ./scripts/check-mainnet-receipt-consistency.mjs",
    "receipt:local": "pnpm -C packages/solana receipt:local",
    "receipt:deploy:devnet": "node ./scripts/deploy-receipt-devnet.mjs",
    "receipt:deploy:mainnet": "node ./scripts/deploy-receipt-mainnet.mjs",
    "positions:devnet": "node ./scripts/find-devnet-whirlpool-positions.mjs",
    "check:anchor-tooling": "node ./scripts/check-anchor-tooling.mjs",
    "shadow:mainnet": "pnpm -C packages/solana shadow:mainnet"
  },
  "devDependencies": {
    "@coral-xyz/anchor": "0.32.1",
    "chai": "^4.5.0",
    "mocha": "^10.8.2",
    "ts-mocha": "^10.0.0",
    "@types/chai": "^4.3.20",
    "@types/mocha": "^10.0.10"
  }
}
